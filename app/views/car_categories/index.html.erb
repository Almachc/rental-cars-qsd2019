<%= link_to t_link(:register_new, :car_category), new_car_category_path %>

<%= flash[:notice] %>

<% @car_categories.each do |category| %>
    <%= link_to category.name, category %>
<% end %>

<%= link_to t_link(:return), root_path %>